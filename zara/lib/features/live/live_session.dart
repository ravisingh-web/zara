// lib/features/live/live_session.dart
// Z.A.R.A. v19 — LiveState bridge
// zara_home_screen.dart imports LiveState from here.

import 'package:zara/services/gemini_live_service.dart';

// Typedef so zara_home_screen can use 'LiveState' directly
typedef LiveState = ZaraLiveState;
